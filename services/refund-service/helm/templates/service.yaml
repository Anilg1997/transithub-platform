apiVersion: v1
kind: Service
metadata:
  name: {{ include "refund-service.fullname" . }}
  labels:
    app: {{ include "refund-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "refund-service.name" . }}
