server: port: 8092
spring:
  application: name: booking-aggregator-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/transithub}?currentSchema=booking_schema
    username: ${DB_USERNAME:transithub}
    password: ${DB_PASSWORD:TransitHub@2024}
  jpa: hibernate: ddl-auto: validate; properties: hibernate: default_schema: booking_schema
  redis: host: ${REDIS_HOST:localhost}; password: ${REDIS_PASSWORD:Redis@2024}
eureka: client: service-url: defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
