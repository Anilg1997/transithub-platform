apiVersion: v1
kind: Service
metadata:
  name: {{ include "train-inventory-service.fullname" . }}
  labels:
    app: {{ include "train-inventory-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "train-inventory-service.name" . }}
