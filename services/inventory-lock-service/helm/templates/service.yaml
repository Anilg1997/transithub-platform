apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-lock-service.fullname" . }}
  labels:
    app: {{ include "inventory-lock-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "inventory-lock-service.name" . }}
