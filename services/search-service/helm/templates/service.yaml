apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-service.fullname" . }}
  labels:
    app: {{ include "search-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "search-service.name" . }}
