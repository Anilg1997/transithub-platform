apiVersion: v1
kind: Service
metadata:
  name: {{ include "flight-booking-service.fullname" . }}
  labels:
    app: {{ include "flight-booking-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "flight-booking-service.name" . }}
