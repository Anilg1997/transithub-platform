apiVersion: v1
kind: Service
metadata:
  name: {{ include "bus-booking-service.fullname" . }}
  labels:
    app: {{ include "bus-booking-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "bus-booking-service.name" . }}
