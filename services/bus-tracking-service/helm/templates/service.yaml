apiVersion: v1
kind: Service
metadata:
  name: {{ include "bus-tracking-service.fullname" . }}
  labels:
    app: {{ include "bus-tracking-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "bus-tracking-service.name" . }}
