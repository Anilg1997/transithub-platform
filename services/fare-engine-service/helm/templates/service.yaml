apiVersion: v1
kind: Service
metadata:
  name: {{ include "fare-engine-service.fullname" . }}
  labels:
    app: {{ include "fare-engine-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "fare-engine-service.name" . }}
